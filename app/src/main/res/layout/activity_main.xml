<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- App Title -->
        <TextView
            android:id="@+id/tv_app_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/app_name"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/primary_color"
            android:layout_marginBottom="24dp"
            android:gravity="center" />

        <!-- Welcome Message -->
        <TextView
            android:id="@+id/tv_welcome"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/welcome_message"
            android:textSize="16sp"
            android:textAlignment="center"
            android:layout_marginBottom="16dp"
            android:background="@drawable/card_background"
            android:padding="16dp" />

        <!-- NDK Information -->
        <TextView
            android:id="@+id/tv_native_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/native_info_placeholder"
            android:textSize="14sp"
            android:fontFamily="monospace"
            android:layout_marginBottom="16dp"
            android:background="@drawable/info_card_background"
            android:padding="16dp"
            android:minHeight="100dp" />

        <!-- OpenCV Information -->
        <TextView
            android:id="@+id/tv_opencv_info"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/opencv_info_placeholder"
            android:textSize="14sp"
            android:layout_marginBottom="24dp"
            android:background="@drawable/info_card_background"
            android:padding="16dp" />

        <!-- Action Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="16dp">

            <Button
                android:id="@+id/btn_camera"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/btn_camera_text"
                android:textSize="16sp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/primary_button_background"
                android:textColor="@android:color/white"
                android:padding="16dp"
                android:drawableStart="@drawable/ic_camera"
                android:drawablePadding="12dp"
                android:gravity="center" />

            <Button
                android:id="@+id/btn_test_native"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/btn_test_native_text"
                android:textSize="16sp"
                android:layout_marginBottom="12dp"
                android:background="@drawable/secondary_button_background"
                android:textColor="@color/primary_color"
                android:padding="16dp"
                android:drawableStart="@drawable/ic_code"
                android:drawablePadding="12dp"
                android:gravity="center" />

        </LinearLayout>

        <!-- Status Information -->
        <TextView
            android:id="@+id/tv_app_version"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Version 1.0 - NDK + OpenCV"
            android:textSize="12sp"
            android:textColor="@android:color/darker_gray"
            android:layout_marginTop="32dp"
            android:gravity="center" />

    </LinearLayout>

</ScrollView>